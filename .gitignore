# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs (local process output only — never committed; cf. the .env tracking incident)
*.log


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local sqlite database
drizzle/
# ignore the api keys / local db, but always ship the static context sample
data/*
!data/context-static.txt
!data/harness-versions.json
